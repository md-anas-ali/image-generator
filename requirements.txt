# CPU-only image generation server using a distilled SD UNet + stock
# CLIP text encoder + stock VAE. No torch, no CUDA, no transformers/optimum
# at runtime — raw onnxruntime instead. Realistic memory floor is ~570MB;
# see app/server.py module docstring and README for the honest numbers.
fastapi==0.115.0
uvicorn[standard]==0.32.0
onnxruntime==1.19.2
numpy==1.26.4
pillow==10.4.0
huggingface-hub==0.25.2   # only used for the (small) one-time file download, not for inference
pydantic==2.9.2
tokenizers==0.20.1        # standalone tokenizer, NOT transformers — encodes the prompt for the text encoder ONNX graph
